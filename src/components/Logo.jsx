export function Logo(props) {
  return (
    <div className="flex items-start justify-center gap-2">
      <span>⚛</span>
      <span className="text-sm font-semibold text-[color:var(--brand-primary)]">ReactField</span>
    </div>
  )
}
